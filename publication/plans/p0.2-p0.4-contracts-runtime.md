# P0.2–P0.4 mini-plan

用户授权连续完成三项；不进入P1。先处理可独立工作，人工决定/硬件门禁异步提出。

- P0.2：基于现有serializer/repository/单track run，固定experiment/run/model/result、事务/stale/路径/兼容契约；schema ADR与旧reader实证。仅文档/contract tooling，不迁移产品代码。
- P0.3：可丢弃的frozen host→external Python worker探针，真实设备/依赖自检、取消/错误/日志验证；有可用环境才执行小DLC train/infer。Windows或真实DLC验证不可获得时保持明确未通过gate，禁止把mock或torch算子当DLC通过。
- P0.4：结合证据更新后续phase入口，独立审查闭环；人工决定有记录；未完成硬件gate按用户决定处置，不能自行勾完。
- 验证：旧repository读v1/拒v2；数据合同正/负例；P0.1证据检查；runtime探针及实际环境报告；独立review；文档链接与diff检查。
- Non-goals：P1四点dataset/train/infer产品接线、科学core、GUI、正式installer、修改科研原件。
- 工作分支：codex/ejp-p0-contracts-runtime；集成目标publication/ejp-damped-pendulum。

## Result

P0.2合同/ADR及旧reader实证完成；P0.3 Mac最终冻结host 8场景符合预期，真实CPU DLC smoke通过，Windows not_run；P0.4独立合同审查PASS、blocking findings闭环。20 tests及P0.1离线证据验证通过。完整状态见[STATUS](../STATUS.md)。用户随后明确批准Windows验证延期至P6前，P0据此完成收尾，Windows仍not_run，不开始P1。
